import 'package:flutter/material.dart';

class CustomoPlansPage extends StatelessWidget {
  const CustomoPlansPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CustomoPlansPage'),
      ),
    );
  }
}
